<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Ghanaian Students Assocation at UNCC Events</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.0.0/dist/css/bootstrap.min.css" integrity="sha384-Gn5384xqQ1aoWXA+058RXPxPg6fy4IWvTNh0E263XmFcJlSAwiGgFAW/dAiS6JXm" crossorigin="anonymous">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css">
    <link rel="stylesheet" href="/css/styles.css">
    <!-- Use absolute or root-relative paths for image file -->
    <link href='/img/GSA-UNCC-logos_white_1.png' rel='stylesheet'>
    <link href='https://unpkg.com/boxicons@2.1.4/css/boxicons.min.css' rel='stylesheet'>
</head>
<body>
<div class="banner-color">
    <nav class="navbar navbar-expand-lg navbar-light banner-color px-5">
        <a class="navbar-brand" href="/"><img class="logo" src="/img/GSA-UNCC-logos_white_1.png" alt=""></a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse ml-auto" id="navbarNavDropdown">
          <ul class="navbar-nav ml-auto">
            <%if(!user) {%>
            <li class="nav-item active">
              <a class="nav-link header-link" href="/users/signup">Sign Up</a>
            </li>
            <li class="nav-item active">
              <a class="nav-link header-link" href="/users/login">Login </a>
            </li>
          <%} else {%>
            <li class="nav-item active">
              <a class="nav-link header-link" href="/events/new">New Events </a>
            </li>
            <li class="nav-item mr-2">
              <a class="nav-link header-link" href="/users/profile"><%= user.firstName %> Profile</a>
            </li>
            <li class="nav-item">
              <a class="nav-link header-link" href="/users/logout">Logout</a>
            </li>
            
          <%}%>
           
          </ul>
        </div>
    </nav>
    <div class="divider"></div>
        <div class="nav-container">
            <div class="nav-links">
                <a href="/">Home</a>
            </div>
            <div class="nav-links">
                <a href="/events">Events</a>
            </div>
        </div>
</div>


<% if(errorMessages.length > 0) {%>
  <% errorMessages.forEach(message=>{%>
      <div class='error'>
          <%=message%>
      </div>
      <%});%>
          <%}%>

              <% if(successMessages.length > 0) {%>

                  <% successMessages.forEach(message=>{%>
                      <div class='success'>
                          <%=message%>
                      </div>
                      <%});%>
                          <%}%>
